<div class="main group-show">
  <div class="container">
    <div class="group">
      <h2><%= @group.name %>の参加メンバー一覧</h2>
     
      <% @group.users.each do |user| %>
        <p><%= user.id %> : <%= user.name %></p>
      <% end %>
    </div>
  </div>
</div>

          <li>
            <%= link_to("投稿一覧", "#{@group.id}/posts/index") %>
          </li>
          <% if @admin %>
            <li>
              <%= link_to("新規投稿", "#{@group.id}/posts/new") %>
            </li>
          <% end %>

<div class="main posts-new">
  <div class="container">        
    <h1 class="form-heading">グループに参加する</h1>
    <%= form_for @group, :url => {:action => 'join'} do |f| %>
      <div class="form">
        <div class="form-body">
          <%= f.collection_select :id, Group.all, :id, :name, :prompt => true %>
          <%= f.submit "送信" %>
        </div>
      </div>
    <% end %>
  </div>
</div>


　　　 